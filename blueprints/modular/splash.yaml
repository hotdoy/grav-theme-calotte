title: Splash
'@extends': modular/splash

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:

        options:
          fields:

            header.optional_classes:
              description: '```header```, ```header_fullscreen```, ```smaller_h1```'